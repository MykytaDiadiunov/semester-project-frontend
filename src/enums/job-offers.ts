export enum SalaryBy {
  hour = 'hour',
  month = 'month',
}

export enum WorkingTimeType {
  fullTime = 'full_time',
  partTime = 'part_time',
}

export enum WorkPlaceType {
  remote = 'remote',
  office = 'office',
  hybrid = 'hybrid',
}
